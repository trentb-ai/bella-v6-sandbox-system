# ⛔ FROZEN — CLEANEST BELLA — DO NOT DEPLOY
# Production-frozen. Source of truth is git tag cleanest-bella.
# All dev work uses -v1-launch or -roi-wip copies. If you are about to run wrangler deploy from this directory — STOP.

name = "fast-intel-v8"
main = "src/index.ts"
compatibility_date = "2025-01-01"
compatibility_flags = ["nodejs_compat"]
account_id = "9488d0601315a70cac36f9bd87aa4e82"

# ─── KV ───────────────────────────────────────────────────────────────────────
[[kv_namespaces]]
binding = "LEADS_KV"
id = "0fec6982d8644118aba1830afd4a58cb"

# ─── Service bindings to downstream workers ───────────────────────────────────
[[services]]
binding = "CONSULTANT"
service = "consultant-v8"

[[services]]
binding = "DEEP_SCRAPE"
service = "deep-scrape-workflow-v9"

[[services]]
binding = "BIG_SCRAPER"
service = "personalisedaidemofinal-sandbox"

# Call Brain DO — event delivery (Phase D)
[[services]]
binding = "CALL_BRAIN"
service = "call-brain-do"

# ─── Secrets (set via: wrangler secret put <NAME>) ───────────────────────────
# FIRECRAWL_API_KEY
# GEMINI_API_KEY  (fallback if Consultant service binding unavailable)

[vars]
# Non-secret vars only

[observability]
enabled = true

[observability.logs]
invocation_logs = true
head_sampling_rate = 1
